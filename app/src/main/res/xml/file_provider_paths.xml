<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <external-files-path name="flight_logs" path="." />
    <files-path name="flight_logs_internal" path="." />
    <!-- Downloaded DataFlash .bin logs are cached here (see AnalyzeLogViewModel). -->
    <cache-path name="downloaded_logs" path="." />
</paths>

